# Helius API key — free tier from https://helius.dev (dashboard → API keys)
# Used for Solana RPC + WebSocket (token detection pipeline)
HELIUS_API_KEY=

# Birdeye API key — free tier from https://bds.birdeye.so
# Used for token price, liquidity, market cap, and OHLCV chart data
BIRDEYE_API_KEY=
